#include <stdio.h>

/**
* main -starting point
* program to print 0-9 using putchar
* Return: value 0
*/
int main(void)
{
	int n;

	for (n = 48; n <= 57; n++)
	putchar(n);
	putchar(10);
	return (0);
}
