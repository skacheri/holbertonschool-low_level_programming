#include "holberton.h"

/**
* mul -function to multiply 2 integers
* @a: 1st number entered by user to multiply
* @b: 2nd number entered by user to multiply
* Return: value of product
*/
int mul(int a, int b)
{
	return (a * b);
}
